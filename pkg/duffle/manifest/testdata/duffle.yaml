name: testbundle
invocationImages:
  cnab:
    name: cnab
    builder: docker
    configuration:
      registry: microsoft
parameters:
  foo:
    type: string
credentials:
  bar:
    path: /tmp
maintainers:
- name: sally

actions:
    hello:
      description: "says hello"
      stateless: true
images:
    test:
      image: "test:latest"
